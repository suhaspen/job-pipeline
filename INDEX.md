# Live postings

0 open · regenerated every run · last updated 2026-08-06 09:03 UTC

`index!` means the link resolves to a careers page rather than the req; `dead!` means it does not resolve at all. Both are treated as expiry signals.

_No live postings yet._

After a cutover the table starts empty by design and fills as new postings appear. Pre-cutover history is in the baseline, which stores ids only.
